create a new item
name > freestyle pipeline >ok
general> description> git hub project > project url >




pipeline {
    agent {label 'jenkins agent'}

    stages {
        stage('code') {
            steps {
                git url: 'https://github.com/LondheShubham153/node-todo-cicd.git', branch: 'master'
            }
        }
        stages {
        stage('build and test') {
            steps {
                echo 'building and testing'
            }
        }
        stages {
        stage('deploy') {
            steps {
                echo 'deploying'
            }
        }
        
    }
}
